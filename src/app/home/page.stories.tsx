import { Meta } from "@storybook/react";
import UserHome from "./page";

const meta: Meta<typeof UserHome> = {
  component: UserHome,
  title: "UserHome",
};

export default meta;

export const StoryUserHome = {};
