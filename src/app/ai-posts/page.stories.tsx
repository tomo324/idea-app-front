import { Meta } from "@storybook/react";
import AiPost from "./page";

const meta: Meta<typeof AiPost> = {
  component: AiPost,
  title: "AiPost",
};

export default meta;

export const StoryAiPost = {};
