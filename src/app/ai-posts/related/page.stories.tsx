import { Meta } from "@storybook/react";
import AiPostRelated from "./page";

const meta: Meta<typeof AiPostRelated> = {
  component: AiPostRelated,
  title: "AiPostRelated",
};

export default meta;

export const StoryAiPostRelated = {};
