import { Meta } from "@storybook/react";
import AiPostButton from "./AiPostButton";

const meta: Meta<typeof AiPostButton> = {
  component: AiPostButton,
  title: "AiPostButton",
};

export default meta;

export const StoryAiPostButton = {};
