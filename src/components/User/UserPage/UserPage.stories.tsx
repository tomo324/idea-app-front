import { Meta } from "@storybook/react";
import UserPage from "./UserPage";

const meta: Meta<typeof UserPage> = {
  component: UserPage,
  title: "UserPage",
};

export default meta;

export const StoryUserPage = {}
