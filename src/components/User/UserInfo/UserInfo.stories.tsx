import { Meta } from "@storybook/react";
import UserInfo from "./UserInfo";

const meta: Meta<typeof UserInfo> = {
  component: UserInfo,
  title: "UserInfo",
};

export default meta;

export const StoryUserInfo = {}
