import { Meta } from "@storybook/react";
import PostButton from "./PostButton";

const meta: Meta<typeof PostButton> = {
  component: PostButton,
  title: "PostButton",
};

export default meta;

export const StoryPostButton = {};
