import { Meta } from "@storybook/react";
import PostList from "./PostList";

const meta: Meta<typeof PostList> = {
  component: PostList,
  title: "PostList",
};

export default meta;

export const StoryPostList = {};
