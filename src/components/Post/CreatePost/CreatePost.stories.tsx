import { Meta } from "@storybook/react";
import CreatePost from "./CreatePost";

const meta: Meta<typeof CreatePost> = {
  component: CreatePost,
  title: "CreatePost",
};

export default meta;

export const StoryCreatePost = {}
